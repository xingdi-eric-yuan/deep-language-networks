v1.0:
  template: |-
    Read the following quiz and provide five different instructions that are commonly helpful in solving all the questions. Make sure to be clear and specific but avoid giving away the answer.

    ## Quiz
    {% for _info in prompt_update_infos %}
    Q{{ loop.index }}:
    {{ _info.input }}
    {% endfor %}

    ## Instruction 1:
    {{ prompt }}

    ## Instruction 2:
    